#include <stdio.h>
int main() {
  int n,i;
  printf("pozitif bir tamsayı giriniz:");
  scanf("%d",&n);
  printf("Çarpanlarına bölünen tamsayı %d:",n);
  for(i=1;i<=n;i++){
    if(n%i==0){
      printf("%d,",i);
    }
  
  } 
  
  return 0;
}
